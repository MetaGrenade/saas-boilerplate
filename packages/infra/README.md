# Infrastructure Package

Use this workspace to define infrastructure-as-code for your SaaS boilerplate. You can integrate
with Terraform, Pulumi, AWS CDK, or any other provisioning tool.

Suggested next steps:

- Configure environment-specific state management.
- Define shared networking, databases, and application hosting resources.
- Wire CI/CD workflows that call the `plan` and `deploy` scripts in this package.
